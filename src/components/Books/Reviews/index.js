import React from "react";
import { FormGroup, Label, Input, Form, Modal } from "bootstrap";
import { useState } from "react";

import "bootstrap/dist/css/bootstrap.min.css";

const Review = ({ props }) => {
  return (
    <div className="container">
      <h1>Reviews</h1>
      <input type="text" />
    </div>
  );
};

export default Review;
